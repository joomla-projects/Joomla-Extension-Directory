---
kind: pipeline
name: default

clone:

steps:
  - name: composer
    image: joomlaprojects/docker-images:php8.2
    volumes:
      - name: composer-cache
        path: /tmp/composer-cache
    commands:
      - composer validate --no-check-all --strict
      - composer install --no-progress --no-suggest

  - name: phpcs
    image: joomlaprojects/docker-images:php8.1
    depends_on: [ composer ]
    commands:
      - echo $(date)
      - ./component/backend/vendor/bin/php-cs-fixer fix -vvv --dry-run --diff
      - ./component/backend/vendor/bin/phpcs --extensions=php -p --standard=ruleset.xml .
      - echo $(date)

volumes:
  - name: composer-cache
    host:
      path: /tmp/composer-cache

---
kind: signature
hmac: 7abf75de936f722f96087502541c671a0ea093bc7022efe933003353505d30be

...
